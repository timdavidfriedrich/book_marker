name: feature_library
version: 0.0.1
environment:
  sdk: ^3.13.1
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core:
  shared:
  flutter_bloc: ^9.1.1
  flutter_hooks: ^0.21.3+1
  injectable: ^3.0.0

flutter:
  generate: false
dev_dependencies:
  build_runner: ^2.16.0
  injectable_generator: ^3.1.3
